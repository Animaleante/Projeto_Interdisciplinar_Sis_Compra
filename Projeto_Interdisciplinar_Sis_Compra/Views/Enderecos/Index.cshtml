@model IEnumerable<Projeto_Interdisciplinar_Sis_Compra.Models.Endereco>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Ingredientes";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.EnderecoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.EnderecoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.EnderecoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
